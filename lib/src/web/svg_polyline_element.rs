pub struct SvgPolylineElement(web_sys::SvgPolylineElement);

/*
impl SvgPolylineElement {
    pub fn new() -> Self {
      pub fn new(node: &str) -> Element {
        let d = Document::new();
        Element(
          d.0.create_element_ns(Some("http://www.w3.org/2000/svg"), "polyline")
            .unwrap(),
        )
      }
    }
}
*/
